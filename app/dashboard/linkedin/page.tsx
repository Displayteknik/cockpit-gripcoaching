import LinkedinMaker from "@/components/LinkedinMaker";

// Admin-vyn av LinkedIn-motorn (full). Kundvyn ligger på /k/linkedin (customerMode).
export default function LinkedinPage() {
  return <LinkedinMaker />;
}
